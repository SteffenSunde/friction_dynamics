#ifndef ODE_HPP
#define ODE_HPP

#include "data_structures.hpp"

//template<typename Derived>
struct ODE {
    //ODE() = 0;
    //auto virtual slope(Eigen::EigenBase<Derived> const& state) -> Eigen::EigenBase<Derived> = 0;
    //virtual ~ODE() {};
};

#endif